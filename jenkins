pipeline {
    agent any

    stages {
        stage('Run pipeline.py') {
            steps {
                sh 'python3 pipeline.py'
                sh 'python3 newfile.py'
            }
        }
    }
}
